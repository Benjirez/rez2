git clone repoAddress master

git status

git add .

git commit -m 'your message'

git push repoAdress branch (ie. master, etc.)

git remote add repoAddress alias (ie. origin)

git push origin master

git pull origin master

// See/Hide .git, un/check Hide VCS Ignored Files (in Settings > Packages > Core Packages > Tree View, see: https://medium.com/@panayiotisgeorgiou/how-to-show-hidden-files-atom-sidebar-c75a2de68709)

// change remote url
git remote set-url origin https://github.com/url...
